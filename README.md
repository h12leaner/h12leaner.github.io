# Leaning Over to Reach Far-Away Objects — project page

Static site, no build step. `index.html` + `style.css` + `assets/`.

Adding the video: drop the mp4 at `assets/lean_reach.mp4` (keep it well
under 100 MB) and swap the marked VIDEO SLOT in `index.html`.
